// «” Œœ«„ Tonal („ «Õ ﬂ‹ window.Tonal)
window.TonalEngine = {
    // Tonal.Chord.detect(['C', 'E', 'G'])
    // Tonal.Scale.get('C major')
    // Tonal.Note.freq('A4')
       Tonal.Interval.distance('C4', 'C5)
    // Tonal.Key.majorKey('C')

    // √—”· «·‰ «∆Ã »‹:
    // window.HybridWebView.SendRawMessage('TONAL_RESULT:' + ...)
};